DATABASE_URL=postgresql+psycopg://localhost:5432/grc_dev
ANTHROPIC_API_KEY=

# Master key for encrypting integration credentials (M365, Nessus, Palo Alto,
# Burp, etc.) at rest in Postgres. Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_ENCRYPTION_KEY=

# Embeddings provider for the knowledge base. Optional for now — falls back
# to Postgres full-text search until set.
VOYAGE_API_KEY=
